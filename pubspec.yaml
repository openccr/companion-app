name: openccr_companion
description: openCCR companion app — diver interface for CCR controllers.
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  # Future features — uncomment when introducing each:
  # flutter_riverpod: ^2.5.1
  # riverpod_annotation: ^2.3.5
  # go_router: ^14.0.0
  # flutter_blue_plus: ^1.33.0
  # permission_handler: ^11.3.1
  # freezed_annotation: ^2.4.4
  # json_annotation: ^4.9.0
  # dio: ^5.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # Future code-gen — uncomment with their dependencies:
  # build_runner: ^2.4.9
  # freezed: ^2.5.2
  # json_serializable: ^6.8.0
  # riverpod_generator: ^2.4.0
  # go_router_builder: ^2.7.0

flutter:
  uses-material-design: true
